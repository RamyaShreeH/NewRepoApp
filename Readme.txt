>Install python

Creation of virtual environment:
>Cmd: python3.11 -m venv directory_name
>cd directory_name
>Activate the environment by Scripts\Activate from the current directory_name

>Libraries to be installed inside the virtual environment:
>pip install flask==2.2.3
>pip install flask-mongoengine

>Then run python file as python app.py in the current directory
